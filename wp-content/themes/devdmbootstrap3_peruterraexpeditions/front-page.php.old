<section id="tours">
  <?php query_posts('cat=23'); ?>
  <?php if (have_posts()): ?>
    <div id="machu-picchu">
      <div class="bg-purple section-title">
        <div class="container">
          <div class="row">
            <div class="col-sm-12">
            <?php
              $lang = get_locale();
              switch ($lang) {
                case 'es_ES': ?>
                  <h3>Tour Machu Picchu</h3><?php
                break;
                case 'en_US': ?>
                  <h3>Machu Picchu Tour</h3> <?php
                break;
              }
            ?>
            </div>
          </div>
        </div>
      </div>
      <div class="tour-content">
        <div class="container">
          <div class="row">
            <?php
            $cuenta = 0;
            while (have_posts()) : the_post();
            ?>
              <div class="col-sm-4 inner-pad">
                <?php if (has_post_thumbnail()): ?>
                  <?php the_post_thumbnail(array(360, 360), array( 'class' => 'img-responsive center-block' )); ?>
                <?php endif ?>
                <h4 class="front-post-title"><?php the_title(); ?></h4>
                <div class="text-justify">
                  <?php the_excerpt(); ?>
                </div>
                <a href="<?php echo "#"; /*the_permalink();*/ ?>" title="<?php echo esc_attr( sprintf( __( 'Permalink to %s', 'devdmbootstrap3' ), the_title_attribute( 'echo=0' ) ) ); ?>" rel="bookmark" class="read-more">
                  <?php
                    $lang = get_locale();
                    switch ($lang) {
                      case 'es_ES': ?>
                        <span>Ver más</span><?php
                      break;
                      case 'en_US': ?>
                        <span>Read more</span><?php
                      break;
                    }
                  ?>
                </a>
                <br>
                <div class="post-meta text-right"><?php echo get_post_meta(get_the_ID(), 'precio', true); ?></div>
              </div>
              <?php $c++; ?>
              <?php if ($c % 3 == 0): ?>
                <div class="clearfix visible-sm-block visible-md-block visible-lg-block"></div>
              <?php endif ?>
            <?php endwhile;?>
          </div>
        </div>
      </div>
    </div>
  <?php endif ?>
  <?php wp_reset_query(); ?>

  <?php query_posts('cat=27'); ?>
  <?php if (have_posts()): ?>
    <div id="cusco-machu-picchu">
      <div class="bg-green section-title">
        <div class="container">
          <div class="row">
            <div class="col-sm-12">
            <?php
              $lang = get_locale();
              switch ($lang) {
                case 'es_ES': ?>
                  <h3>Tour Cusco y Machu Picchu</h3><?php
                break;
                case 'en_US': ?>
                  <h3>Cusco and Machu Picchu Tour</h3> <?php
                break;
              }
            ?>
            </div>
          </div>
        </div>
      </div>
      <div class="tour-content">
        <div class="container">
          <div class="row">
            <?php
            $cuenta = 0;
            while (have_posts()) : the_post();
            ?>
              <div class="col-sm-4 inner-pad">
                <?php if (has_post_thumbnail()): ?>
                  <?php the_post_thumbnail('front-size', array( 'class' => 'img-responsive center-block' )); ?>
                <?php endif ?>
                <h4 class="front-post-title"><?php the_title(); ?></h4>
                <div class="text-justify">
                  <?php the_excerpt(); ?>
                </div>
                <a href="<?php echo "#"; /*the_permalink();*/ ?>" title="<?php echo esc_attr( sprintf( __( 'Permalink to %s', 'devdmbootstrap3' ), the_title_attribute( 'echo=0' ) ) ); ?>" rel="bookmark" class="read-more">
                  <?php
                    $lang = get_locale();
                    switch ($lang) {
                      case 'es_ES': ?>
                        <span>Ver más</span><?php
                      break;
                      case 'en_US': ?>
                        <span>Read more</span><?php
                      break;
                    }
                  ?>
                </a>
                <br>
                <div class="post-meta text-right"><?php echo get_post_meta(get_the_ID(), 'precio', true); ?></div>
              </div>
              <?php $c++; ?>
              <?php if ($c % 3 == 0): ?>
                <div class="clearfix visible-sm-block visible-md-block visible-lg-block"></div>
              <?php endif ?>
            <?php endwhile;?>
          </div>
        </div>
      </div>
    </div>
  <?php endif ?>
  <?php wp_reset_query(); ?>

  <?php query_posts('cat=31'); ?>
  <?php if (have_posts()): ?>
    <div id="tren">
      <div class="bg-gray section-title">
        <div class="container">
          <div class="row">
            <div class="col-sm-12">
            <?php
              $lang = get_locale();
              switch ($lang) {
                case 'es_ES': ?>
                  <h3>Tren Inca y Otras Rutas</h3><?php
                break;
                case 'en_US': ?>
                  <h3>Inca Train and Others</h3><?php
                break;
              }
            ?>
            </div>
          </div>
        </div>
      </div>
      <div class="tour-content">
        <div class="container">
          <div class="row">
            <?php
            $cuenta = 0;
            while (have_posts()) : the_post();
            ?>
              <div class="col-sm-4 inner-pad">
                <?php if (has_post_thumbnail()): ?>
                  <?php the_post_thumbnail('front-size', array( 'class' => 'img-responsive center-block' )); ?>
                <?php endif ?>
                <h4 class="front-post-title"><?php the_title(); ?></h4>
                <div class="text-justify">
                  <?php the_excerpt(); ?>
                </div>
                <a href="<?php echo "#"; /*the_permalink();*/ ?>" title="<?php echo esc_attr( sprintf( __( 'Permalink to %s', 'devdmbootstrap3' ), the_title_attribute( 'echo=0' ) ) ); ?>" rel="bookmark" class="read-more">
                  <?php
                    $lang = get_locale();
                    switch ($lang) {
                      case 'es_ES': ?>
                        <span>Ver más</span><?php
                      break;
                      case 'en_US': ?>
                        <span>Read more</span><?php
                      break;
                    }
                  ?>
                </a>
                <br>
                <div class="post-meta text-right"><?php echo get_post_meta(get_the_ID(), 'precio', true); ?></div>
              </div>
              <?php $c++; ?>
              <?php if ($c % 3 == 0): ?>
                <div class="clearfix visible-sm-block visible-md-block visible-lg-block"></div>
              <?php endif ?>
            <?php endwhile;?>
          </div>
        </div>
      </div>
    </div>
  <?php endif ?>
  <?php wp_reset_query(); ?>

  <?php query_posts('cat=35'); ?>
  <?php if (have_posts()): ?>
    <div id="cusco">
      <div class="bg-gray section-title">
        <div class="container">
          <div class="row">
            <div class="col-sm-12">
            <?php
              $lang = get_locale();
              switch ($lang) {
                case 'es_ES': ?>
                  <h3>Tour en Cusco</h3><?php
                break;
                case 'en_US': ?>
                  <h3>Cusco Tours</h3> <?php
                break;
              }
            ?>
            </div>
          </div>
        </div>
      </div>
      <div class="tour-content">
        <div class="container">
          <div class="row">
            <?php
            while (have_posts()) : the_post();
            ?>
              <div class="col-sm-4 inner-pad">
                <?php if (has_post_thumbnail()): ?>
                  <?php the_post_thumbnail(array(360, 360), array( 'class' => 'img-responsive center-block' )); ?>
                <?php endif ?>
                <h4 class="front-post-title"><?php the_title(); ?></h4>
                <div class="text-justify">
                  <?php the_excerpt(); ?>
                </div>
                <a href="<?php echo "#"; /*the_permalink();*/ ?>" title="<?php echo esc_attr( sprintf( __( 'Permalink to %s', 'devdmbootstrap3' ), the_title_attribute( 'echo=0' ) ) ); ?>" rel="bookmark" class="read-more">
                  <?php
                    $lang = get_locale();
                    switch ($lang) {
                      case 'es_ES': ?>
                        <span>Ver más</span><?php
                      break;
                      case 'en_US': ?>
                        <span>Read more</span><?php
                      break;
                    }
                  ?>
                </a>
                <br>
                <div class="post-meta text-right"><?php echo get_post_meta(get_the_ID(), 'precio', true); ?></div>
              </div>
            <?php endwhile;?>
          </div>
        </div>
      </div>
    </div>
  <?php endif ?>
  <?php wp_reset_query(); ?>
</section>